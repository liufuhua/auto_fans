# API base path. Use /api for Vite proxy or a full backend URL such as http://127.0.0.1:8000/api.
VITE_API_BASE_URL=/api

# Keep mock enabled while the FastAPI backend is not ready.
# Set both to false after backend APIs are available.
VITE_USE_MOCK_API=true
VITE_USE_MOCK_AUTH=true
