[pytest]
addopts = -ra
testpaths = tests
pythonpath = src
markers =
    appium: tests that require an Appium server and connected Android device
    page_spec: page specification and UI contract checks
